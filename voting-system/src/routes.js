import Vote from "./Views/Vote.jsx";
import Setup from "./Views/Setup.jsx";

const RouteObjects = [
    {
        path: "/vote",
        component: Vote,
    },
    {
        path: "/setup",
        component: Setup,
    }
];

export default RouteObjects;